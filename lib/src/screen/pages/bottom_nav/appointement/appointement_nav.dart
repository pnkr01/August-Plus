import 'package:flutter/material.dart';

class ApoointementNav extends StatelessWidget {
  const ApoointementNav({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}