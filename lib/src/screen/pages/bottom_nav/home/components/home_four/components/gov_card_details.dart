import 'package:flutter/material.dart';

class GovDetailsHealthCard extends StatelessWidget {
  const GovDetailsHealthCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
